# Bounded development: retention and conjunctive revision

2026-09-14. Development only; commit before generation. Reuse CE-128 from
procedure-transfer evidence c183674, publication dcdc0d6. All source acquisition
attempts remain disclosed through the pinned diagnosis; selection is the published
earliest >=15/16 checkpoint, not this study's transfer scores.

Original A: copper→kestrel, violet→marten; uppercase identifier; -Q iff fast.
Later B: amber→marten, teal→kestrel, same argument rule. Four new training
identifiers, all four combinations. Revision C: ONLY copper AND fast now routes
to marten. Priority still controls -Q; all other routes and arguments unchanged.
This is a small conditional-routing calibration, not a novel editing algorithm.
No multi-hop or environment execution claim is made from emitted calls.

Starting CE checkpoint: verify all 16 original recalls after loading with no
teaching context. Development identifiers are fresh and excluded from later tests.
B-only: 128 checked-answer CE updates. B-replay: identical 128 B updates plus
128 interleaved original A updates. Both use AdamW 0.0005, zero weight decay,
reset optimizer at each stage; same rank-8 last-eight-block q/v LoRA runtime.
From B-replay, C-only gets 128 correction updates; C-replay gets identical
correction updates plus 128 interleaved unaffected A/B updates. C examples use
four new identifiers; replay excludes stale copper/fast examples. At 32 and 128
new-target updates assess training and development route, format, identifier,
suffix and full-call correctness. Save all checkpoints. Do not call ineffective B
training a retention test. If B or C fails routing recall, diagnose with bounded
rate/update/replay controls before confirmatory work. No intermediate checkpoint
selection based on final material. A fixed endpoint will be declared separately.

Explicit evidence reference: base with original examples plus later examples,
with superseded copper/fast records replaced by corrected labels for revised
queries; generic induction reminder. No complete rule supplied. It has more
inference context than students. Also measure no-evidence base. No timing or
memory-placement superiority claim without accounting for unequal access.

Peak MLX ceiling 40 GB, 20-minute execution ceiling. Wait for observed sibling
training to finish; do not stop others' jobs. Record process snapshots before
and after. One local MLX process at a time. No paid model calls or weight download.
