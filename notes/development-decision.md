# Development result and final decision

Development-v2 source22fd99e; completed with816 generations,768 updates, eight
saved checkpoints; about535 seconds active plus810 seconds waiting for shared
hardware. The first attempt remains a separate resource interruption with30
logged updates and136 generations. No attempt is removed from the record.

CE acquisition recall16/16 reproduced after reload. B-only learned16/16 new
calls by32 updates while old routing stayed16/16; continuing to128 kept B16/16
but changed all copper routes to marten (old routing8/16). B-replay retained
16/16 full old and new calls at128. New development identifiers still had
only12/16 complete calls in each channel family despite16/16 routing.

C-only learned4/4 corrected calls but generalized marten to copper/slow and
teal; early32 did not avoid that pattern. C-replay also learned4/4, preserving
much more routing, but its128 endpoint reached only15/16 routes in each fresh
development family, with7/16 and8/16 full calls. Its32 endpoint had all routes
correct and zero whole calls on those same new identifiers. Thus neither
component preservation nor a transient diagnostic endpoint is complete-task
retention. Do not select that32 checkpoint from its routing score.

All training acquisition criteria are met, so no acquisition repair is needed.
Proceed to both published acquired states using the frozen final protocol,
retaining every arm. The development-informed additions are early32 checkpoints
for B-only/C-only and separately privileged complete-rule references after the
example-context reference struggled on B. The revised final example reference
removes four obsolete A records and adds the four actual corrections, eliminating
the development reference's extra corrected labels. No final outputs informed
these changes. Final task has12 fresh identifiers crossed with eight conditions.

Interpretation remains limited: positive correction examples do not uniquely
identify scope; replay adds boundary information. The experiment studies
conditional routing and argument production, not independent editable parameters,
long-term lifelong retention, task execution in an external environment, or
memory-placement optimality. Full final counts, not these development scores,
will support the fresh-input findings.
