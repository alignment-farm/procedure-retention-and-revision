# Explicit-memory reference repair

Prospective addition during development, 15 September 2026. The first 160
unadapted complete-rule prompts produced only 85 complete procedures. Errors
include emitting SHIP despite SKIP reservation and changing held stock to zero.
This is a failed natural-language executor, not evidence that explicit policy
storage itself cannot preserve the procedure. Preserve all those generations.

Add a competent executable-policy reference: a JSON eligibility expression plus
a fixed reservation/shipment transition interpreter. Authoritative revisions
append exactly one scoped eligibility clause at each boundary. No model calls,
learning, example search, or autonomous rule extraction. This is privileged
investigator-authored formal policy information, stronger than labeled examples;
construction and correction authority are supplied. Its small closed-domain
implementation is an intentional limitation, not a general replacement for LLMs.

Run both recurring passes on the same workload and check every emitted plan using
the independent environment checker, including all intermediate state assertions.
Record policy snapshots/bytes, revision clause additions, parsing/maintenance and
execution time, Python/runtime/source revision and zero neural updates/tokens.
Keep the failed prompted-rule reference as a separate reported comparator.
Development of this interpreter is experimental design effort, not free inferred
knowledge; no investigator-hour or monetary estimate will be invented.
