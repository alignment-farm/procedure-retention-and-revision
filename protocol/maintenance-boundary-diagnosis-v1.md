# Bounded negative-boundary diagnosis

Fixed during development, after the novel-only arm's two endpoints and before
this diagnostic's generations. That arm corrected every new obligation but lost
28 unchanged successes at revision 1 and ended at 120/160 full tasks at revision 2.
Errors are overgeneralized eligibility with downstream shipment, not string syntax.

One further development comparison is authorized locally: restore the SAME saved
seed311 A256 checkpoint, run each identity arm for revision 1 only, keeping 64
correction and 64 identical historical updates. Replace the 64 broadly cycled
boundary updates with 64 unchanged ineligible orders, across the same arm-specific
entities. This changes the content and frequency of scope-negative evidence,
not the number of updates, correction labels or historical access. Do not call it
an information-equivalent comparison or isolate content from example frequency.
No adaptation of correction authority or autonomous example selection is claimed.

Evaluate the same 160 development orders once; original repeated-query checks are
already retained. Selection for final: choose negative boundaries only if BOTH
identity arms retain all 20 revision-1 changed obligations and neither arm's
unchanged complete score is worse than its corresponding original endpoint,
with at least one strict improvement. Otherwise retain the original all-boundary
recipe. Freeze final independent adapter seeds 401/402 and fresh entities
wexlun, pazdor, jivrek, qomset after the diagnostic; no final response may choose
an endpoint. No further correction-recipe search is planned. This is explanatory
calibration; fresh evaluation tests the selected recipe, not a fresh replication
of the boundary-frequency causal effect.

Runner logs/guards and model provenance follow maintenance-development-v1.
No hardware load until the first development run completes; one active-hour and
two-hour shared-resource wait ceiling. Save exact sources and starting hash.
